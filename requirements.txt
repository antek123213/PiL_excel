# External dependencies required by the project.
# Standard library modules (json, argparse, datetime, etc.) are not listed here.
openpyxl
psycopg2-binary
python-dateutil
pywin32  # Required for VBA insertion into .xlsm files (Windows only)
